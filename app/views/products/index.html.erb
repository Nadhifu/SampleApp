<h1>PRODUCTS</h1>

<table>
  <tr class=trhead >
    <th>Sku XXX</th>
    <th>Description</th>
    <th>Price</th>
    <th>Action</th>
    <th>Action</th>
    <th>Action</th>
  </tr>

<% @products.each do |product| %>
  <tr class=trdetail>
    <td><%= product.sku %></td>
    <td><%= product.description %></td>
    <td><%= product.price %></td>
    <td><%= link_to 'Show', product %></td>
    <td><%= link_to 'Edit', edit_product_path(product) %></td>
    <td><%= link_to 'Destroy', product, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'NEW PRODUCT', new_product_path %>    <%= link_to 'CUSTOMERS INDEX', customers_path %>
